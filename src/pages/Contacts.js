import styled from "styled-components";

const Container = styled.div`
    display: flex;
    flex-direction: row;
    width: 100vw;
    height: 100vh;
    `

const Contacts = () => {
    return ( 
        <Container></Container>
    );
}
 
export default Contacts;